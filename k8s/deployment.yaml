apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-deploy
  namespace: demo-tst
spec:
  replicas: 1
  revisionHistoryLimit: 1
  selector:
    matchLabels:
      app: hello
  template:
    metadata:
      labels:
        app: hello
    spec:
      containers:
        - name: hello
          image: httpd:latest
          ports:
            - containerPort: 80
          env:
            - name: teste
              value: "jorge"